class WidgetStateArg {
  bool isTextSelected = false;
  bool isImageSelected = false;
  bool isButtonSelected = false;

  WidgetStateArg(
      this.isTextSelected, this.isImageSelected, this.isButtonSelected);
}
